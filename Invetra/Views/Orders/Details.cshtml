@* @model Inventra.Models.Orders.OrderDetailsViewModel

@{
    ViewData["Title"] = "Order Details";
}

<div class="container mt-4">
    <h1>Details</h1>

    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">Order Information</h4>
        </div>
        <div class="card-body">
            <hr />
            <dl class="row">
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.CustomerName)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.CustomerName)
                </dd>

                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.CourierName)
                </dt>
                <dd class="col-sm-9">
                    @if (string.IsNullOrEmpty(Model.CourierName))
                    {
                        <span class="text-muted italic">No courier assigned</span>
                    }
                    else
                    {
                        @Html.DisplayFor(model => model.CourierName)
                    }
                </dd>

                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.TrackingNumber)
                </dt>
                <dd class="col-sm-9 text-monospace">
                    @Html.DisplayFor(model => model.TrackingNumber)
                </dd>

                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.TotalPrice)
                </dt>
                <dd class="col-sm-9 fw-bold text-success">
                    @Model.TotalPrice.ToString("C")
                </dd>

                @if (!string.IsNullOrEmpty(Model.AdditionalInfo))
                {
                    <dt class="col-sm-3">
                        @Html.DisplayNameFor(model => model.AdditionalInfo)
                    </dt>
                    <dd class="col-sm-9">
                        @Html.DisplayFor(model => model.AdditionalInfo)
                    </dd>
                }
            </dl>
        </div>
        <div class="card-footer bg-light">
            <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning">Edit Order</a>
            <a asp-action="Index" class="btn btn-secondary ms-2">Back to List</a>
            <a asp-controller="OrderDetails"
               asp-action="Create"
               asp-route-orderId="@Model.Id"
               class="btn btn-primary mt-3">
                + Add Product
            </a>
        </div>
    </div>
</div>
 *@
@model Inventra.Models.Orders.OrderDetailsViewModel

@{
    ViewData["Title"] = "Order Details";
}

<h1>Order Details</h1>

<div>
    <h4>Order Summary</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Customer Name</dt>
        <dd class="col-sm-10">@Model.CustomerName</dd>

        <dt class="col-sm-2">Tracking Number</dt>
        <dd class="col-sm-10">@Model.TrackingNumber</dd>

        <dt class="col-sm-2">Additional Information</dt>
        <dd class="col-sm-10">@Model.AdditionalInfo</dd>

        <dt class="col-sm-2">Order Total</dt>
        <dd class="col-sm-10 fw-bold text-success">@Model.TotalPrice.ToString("N2") €</dd>
    </dl>
</div>

<hr class="mt-4 mb-4" />

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4>Products in this Order</h4>

    <a asp-controller="OrderDetails"
       asp-action="Create"
       asp-route-orderId="@Model.Id"
       class="btn btn-primary">
        + Add Product
    </a>
</div>

<table class="table table-bordered table-striped mt-3">
    <thead class="table-dark">
        <tr>
            <th>Product Name</th>
            <th class="text-end">Quantity (QTY)</th>
            <th class="text-end">Unit Price</th>
            <th class="text-end">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Products != null && Model.Products.Count > 0)
        {
            @* 🟢 FIX: We explicitly tell the page exactly what 'item' is! *@
            @foreach (Inventra.Data.Entities.OrderDetails item in Model.Products)
            {
                <tr>
                    <td class="align-middle">@item.Product?.Name</td>

                    <td class="text-end align-middle">@item.QTY</td>

                    <td class="text-end align-middle">
                        @if (item.QTY > 0)
                        {
                            @((item.Subtotal / item.QTY).ToString("N2"))
                        }
                        else
                        {
                            @("0.00")
                        }
         €
                    </td>

                    <td class="text-end align-middle fw-bold">@item.Subtotal.ToString("N2") €</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="4" class="text-center text-muted p-4">
                    <em>No products have been added to this order yet.</em>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="mt-4">
    <a asp-action="Index" class="btn btn-outline-secondary">Back to Orders List</a>
</div>