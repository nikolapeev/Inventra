@model IEnumerable<Inventra.Core.ViewModels.Couriers.CourierIndexViewModel>

@{
    ViewData["Title"] = "Couriers";
}

<div class="d-flex justify-content-between align-items-end mb-4">
    <div>
        <h1 class="h3 mb-1 text-dark fw-bold">Couriers</h1>
        <p class="text-muted mb-0">Manage your delivery partners and contact numbers.</p>
    </div>
    <div>
        <a asp-action="Create" class="btn btn-primary shadow-sm rounded-3">
            <i class="bi bi-plus-lg me-1"></i> Add Courier
        </a>
    </div>
</div>

<div class="card border-0 shadow-sm rounded-4 overflow-hidden">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light text-muted small text-uppercase">
                    <tr>
                        <th class="ps-4 py-3" style="width: 40%;">Courier Name</th>
                        <th class="py-3">Contact Phone</th>
                        <th class="text-end pe-4 py-3" style="width: 20%;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="3" class="text-center py-5 text-muted">
                                <i class="bi bi-bicycle fs-2 d-block mb-2"></i>
                                No couriers found. Click "Add Courier" to create one.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 fw-bold text-dark py-3">
                                    <div class="d-flex align-items-center">
                                        <div class="bg-secondary bg-opacity-10 text-secondary rounded d-flex justify-content-center align-items-center me-3" style="width: 35px; height: 35px;">
                                            <i class="bi bi-bicycle"></i>
                                        </div>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </div>
                                </td>
                                <td class="py-3 text-muted">
                                    <i class="bi bi-telephone text-secondary me-1"></i> @Html.DisplayFor(modelItem => item.Phone)
                                </td>
                                <td class="text-end pe-4 py-3">
                                    <div class="btn-group btn-group-sm shadow-sm" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.CourierId" class="btn btn-outline-primary" title="Edit">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <form asp-action="Delete" asp-route-id="@item.CourierId" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this courier?');">
                                            <button type="submit" class="btn btn-outline-danger" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>